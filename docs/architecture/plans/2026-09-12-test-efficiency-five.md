# Five-target test efficiency pass

Base: `65b555341` on develop. Task branch `feature/ai-test-efficiency-five`.

1. Measure sidecar, constructor guard, trace/movie guard, FBZ matrix/standalone,
   master relay, and verified-room integration on the unchanged base.
2. Reuse immutable S3K reference/capture results. Prove old prefix captures equal
   full-capture prefixes; retain perturbation independence and every distinct pin.
3. Cache constructor-guard extraction and transitive analysis without narrowing
   source scope. Compare indexes/violations and exercise negative fixtures.
4. Consolidate duplicate FBZ route execution only after moving unique assertions
   into surviving canonical configuration checks. Retain distinct configurations,
   donor reset checks, starpost tests, and exact baseline failure attribution.
5. Parse only the required CSV column and reuse immutable input data across trace
   alignment checks. Verify byte/row equivalence and malformed-input behavior.
6. Replace network sleeps with controlled room time or observed events, preserving
   real relay connections, sanctions, verified uploads and trust rejection.
7. Complete focused checks, record measurements and retirement mapping, then run
   one required change-based selection with preflight. Attribute failures narrowly.
8. Integrate into develop, check the merged tree, push develop, and remove the clean
   merged task worktree/branch. Preserve unrelated main-workspace changes.

No test is retired solely because it is slow or currently fails. Removed executions
must map to retained assertions and equivalent configuration/trajectory coverage.
Large oracle inputs, ROM routes, all unique configurations and gate scope remain.
Temporary diagnostics are consumed and deleted; durable evidence is a concise
validation ledger under docs/architecture/validation/.

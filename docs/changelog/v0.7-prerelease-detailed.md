# OpenGGF 0.7 — Development ledger

## 2026-09-12 branch rollover

- Preserve `release/0.6.20260911` at published tag commit `37aeb6b84`.
- Synchronize the release fixes from `master` into `develop` before promotion.
- Merge the synchronized development history into the existing `next` work,
  then bring that combined history back to `develop` as `0.7.prerelease`.
- Advance `next` to `0.8.prerelease` after the back-merge, keeping the Mod API
  candidate version and unpublished state unchanged; its branch topology follows the rollover.

Verification and integration outcomes are recorded in the
[rollover validation record](../architecture/validation/2026-09-12-release-rollover.md).

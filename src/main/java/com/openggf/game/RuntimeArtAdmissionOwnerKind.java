package com.openggf.game;

/** Production owner permitted to consume a runtime-art admission lease. */
public enum RuntimeArtAdmissionOwnerKind {
    IMMEDIATE,
    TITLE_OWNER,
    RESOURCE_HANDOFF_OWNER
}
